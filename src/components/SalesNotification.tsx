// ⚠️ Componente DESACTIVADO por decisión del usuario.
// La plantilla original está guardada en: .lovable/templates/SalesNotification.tsx.bak
// Para reactivar: copia el contenido del .bak de vuelta a este archivo.

interface SalesNotificationProps {
  lang?: string;
  productKey?: string;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
const SalesNotification = (_props: SalesNotificationProps) => null;

export default SalesNotification;
